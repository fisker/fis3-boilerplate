<%
const $ = require('../../snippets/jst')

const page = {
  head: {
    title: 'tab panel',
    keywords: [
      ...(project.keywords || []),
    ],
    description: '',
    styles: [
      ...(project.styles || []),
    ],
    scripts: [],
  },
  header: {},
  footer: {
    scripts: [
      '/project/scripts/config.js.jst',
      ...(project.scripts || []),
      '../../assets/scripts/component/_tab-panel.js?__inline',
    ]
  }
}
%>
<!DOCTYPE html>
<%= $.htmlStartTag('') %>
  <head>
    <%= $.head(page.head) %>

    <style type="text/x-scss">
      @import '../../assets/styles/global';
      @import '../../assets/styles/component/tab-panel';

      .demo {
        padding:  15px;
      }

      .u-tab-panel__tab {
        margin-bottom: -1px;
        padding: 5px 10px;
        cursor: pointer;
      }

      .u-tab-panel__tab_current {
        background: #ccc;
      }

      .u-tab-panel__content {
        padding: 10px;
        border: 1px solid #ccc;
      }
    </style>
  </head>
  <body>

    <div class="container demo">
      <legend>.u-tab-panel (css、js)</legend>
      <div class="u-tab-panel">
        <h4 class="u-tab-panel__tab">tab1</h4>
        <div class="u-tab-panel__panel">
          <div class="u-tab-panel__content">
            content 1
          </div>
        </div>
        <h4 class="u-tab-panel__tab">tab2</h4>
        <div class="u-tab-panel__panel">
          <div class="u-tab-panel__content">
            content 2
          </div>
        </div>
      </div>
    </div>
    <%= $.footer(page.footer) %>
  </body>
<%= $.htmlEndTag() %>
